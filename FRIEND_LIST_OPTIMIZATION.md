# 好友列表显示优化

## 优化内容

### 1. 可能认识列表优化

**显示逻辑**：
- **有昵称时**：显示 `昵称`，副标题显示 `@用户名`
- **无昵称时**：显示 `用户名`，副标题显示 `微信用户`

**头像显示**：
- 有微信头像时显示圆形头像图片
- 无头像时显示首字母占位符

**示例**：
```
[头像] 张三               加好友
       @zhangsan

[头像] zhangsan           加好友
       微信用户
```

### 2. 好友请求列表优化

- 显示发送者头像（微信头像或首字母）
- 显示发送者昵称（优先）或用户名
- 显示用户名（如果有昵称）

### 3. 好友列表优化

- 显示好友头像（微信头像或首字母）
- 显示好友昵称（优先）或用户名
- 显示成为好友日期

---

## 文件修改

### 后端
- `src/app/api/friends/route.ts`
  - 优化 `displayName` 格式（仅昵称，不再拼接用户名）
  - 添加 `avatar` 字段返回

### 前端
- `weapp/pages/friends/friends.js`
  - 优化 `loadSuggestions` 数据处理逻辑
  - 添加 `subtitle` 和 `hasAvatar` 字段

- `weapp/pages/friends/friends.wxml`
  - 三个列表（可能认识、好友请求、好友列表）都添加头像图片支持
  - 优化显示结构和样式

- `weapp/pages/friends/friends.wxss`
  - 添加 `.user-avatar-img` 样式
  - 头像图片圆形裁剪、固定尺寸

---

## 数据流

```
微信登录 → 获取微信 nickName + avatarUrl → 保存到数据库
                                           ↓
可能认识列表 ← 查询所有用户 ← 后端 API ← 包含 nickname + avatar
                                           ↓
前端渲染 → 有 avatar 显示图片 : 无 avatar 显示首字母
         → 有 nickname 显示昵称 : 无 nickname 显示用户名
```

---

## 需要重新部署

修改了后端代码，需要重新部署 CloudRun 服务。

---

## 测试清单

- [ ] 有昵称+头像的用户正确显示头像和昵称
- [ ] 无昵称的用户显示用户名和"微信用户"标签
- [ ] 无头像的用户显示首字母占位符
- [ ] 三个列表（可能认识、请求、好友）都正常显示
