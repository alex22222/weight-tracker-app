// 配置文件
const config = {
  // API 基础地址 - 开发环境使用本地服务器
  // 生产环境需要替换为实际域名
  apiBaseUrl: 'http://localhost:54112/api',
  
  // 请求超时时间（毫秒）
  timeout: 10000,
  
  // 版本号
  version: '1.0.0'
}

module.exports = config
